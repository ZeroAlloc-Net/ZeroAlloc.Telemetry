# ZeroAlloc.Telemetry

Source-generated OpenTelemetry instrumentation — `Activity` spans and `Meter` instruments without reflection or `params object[]` allocation.

## Install

```bash
dotnet add package ZeroAlloc.Telemetry
```
